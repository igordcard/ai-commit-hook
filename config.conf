# ai-commit-hook configuration
# These values are baked into the hook at install time (via install.sh).
# After changing values, re-run ./install.sh to apply.

# Model to use for commit reviews (e.g., sonnet, opus, haiku)
MODEL=sonnet
